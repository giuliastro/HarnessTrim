{
  "name": "harnesstrim",
  "private": true,
  "version": "0.0.1",
  "description": "One token policy for Claude Code, Codex, OpenCode and Pi.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck",
    "bench": "pnpm --filter @harnesstrim/benchmarks run bench"
  },
  "devDependencies": {
    "@harnesstrim/cli": "workspace:*",
    "@types/node": "^26.1.1",
    "tsx": "^4.19.0",
    "typescript": "^7.0.2"
  }
}
